{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's profile in the application.",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The user's unique ID from Firebase Authentication."
        },
        "numericId": {
          "type": "string",
          "description": "A randomly generated 8-digit numeric ID for the user."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address."
        },
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "photoURL": {
          "type": "string",
          "format": "uri",
          "description": "URL of the user's profile picture."
        },
        "phoneNumber": {
          "type": "string",
          "description": "The user's phone number."
        },
        "balance": {
          "type": "number",
          "description": "The user's account balance."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the user profile was created."
        },
        "paymentMethods": {
          "type": "array",
          "description": "A list of the user's linked payment methods.",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "description": "The name of the payment method (e.g., PhonePe, Paytm)."
              },
              "upiId": {
                "type": "string",
                "description": "The user's UPI ID for that payment method."
              }
            },
            "required": ["name", "upiId"]
          }
        }
      },
      "required": ["uid", "numericId", "balance", "createdAt", "phoneNumber"]
    },
    "AdminPaymentMethod": {
      "title": "Admin Payment Method",
      "description": "Stores payment method details configured by the admin.",
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "enum": ["bank", "upi"]
        },
        "bankName": {
          "type": "string",
          "description": "The name of the bank."
        },
        "accountHolderName": {
          "type": "string",
          "description": "The name of the account holder for bank transfer."
        },
        "accountNumber": {
          "type": "string",
          "description": "The bank account number."
        },
        "ifscCode": {
          "type": "string",
          "description": "The IFSC code of the bank."
        },
        "upiHolderName": {
          "type": "string",
          "description": "The name of the UPI account holder."
        },
        "upiId": {
          "type": "string",
          "description": "The UPI ID."
        }
      },
      "required": ["type"]
    },
    "Order": {
      "title": "Order",
      "description": "Represents a user's request to buy LGB.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who created the order."
        },
        "orderId": {
          "type": "string",
          "description": "A unique, randomly generated ID for the order, prefixed with 'LGPAY'."
        },
        "amount": {
          "type": "number",
          "description": "The amount of LGB to buy, in INR."
        },
        "paymentType": {
          "type": "string",
          "description": "The payment method type chosen by the user.",
          "enum": ["bank", "upi"]
        },
        "paymentProvider": {
          "type": "string",
          "description": "The specific UPI provider chosen (e.g., PhonePe)."
        },
        "adminPaymentMethodId": {
          "type": "string",
          "description": "The ID of the admin payment method used for the transaction."
        },
        "status": {
          "type": "string",
          "description": "The current status of the order.",
          "enum": [
            "pending_payment",
            "processing",
            "completed",
            "cancelled",
            "failed"
          ]
        },
        "cancellationReason": {
          "type": "string",
          "description": "The reason provided by the user for cancelling the order."
        },
        "utr": {
          "type": "string",
          "description": "The UTR/reference number of the payment."
        },
        "screenshotURL": {
          "type": "string",
          "format": "uri",
          "description": "URL of the uploaded payment screenshot."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the order was created."
        },
        "submittedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the user submitted payment proof."
        }
      },
      "required": ["userId", "amount", "status", "createdAt", "orderId"]
    },
     "SellOrder": {
      "title": "Sell Order",
      "description": "Represents a user's request to sell LGB and withdraw money.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user who created the sell order."
        },
        "userNumericId": {
          "type": "string",
          "description": "The numeric ID of the user who created the order."
        },
        "userPhoneNumber": {
            "type": "string",
            "description": "The phone number of the user who created the order."
        },
        "orderId": {
          "type": "string",
          "description": "A unique, randomly generated ID for the order, prefixed with 'LGPAY'."
        },
        "amount": {
          "type": "number",
          "description": "The amount of LGB to sell, in INR."
        },
        "withdrawalMethod": {
          "type": "object",
          "description": "The UPI method chosen by the user for withdrawal.",
          "properties": {
            "name": {
              "type": "string",
              "description": "The name of the UPI provider (e.g., PhonePe)."
            },
            "upiId": {
              "type": "string",
              "description": "The UPI ID for withdrawal."
            }
          },
          "required": ["name", "upiId"]
        },
        "status": {
          "type": "string",
          "description": "The current status of the sell order.",
          "enum": [
            "pending",
            "processing",
            "completed",
            "failed"
          ]
        },
        "utr": {
            "type": "string",
            "description": "The UTR/reference number of the payment sent by the admin."
        },
        "failureReason": {
            "type": "string",
            "description": "Reason for a failed or cancelled sell order."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the order was created."
        }
      },
      "required": ["userId", "amount", "withdrawalMethod", "status", "createdAt", "orderId", "userNumericId", "userPhoneNumber"]
    },
    "ChatRequest": {
      "title": "Chat Request",
      "description": "Represents a user's request to chat with a human agent.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The Firebase Auth UID of the user who requested the chat, if they are logged in."
        },
        "userNumericId": {
          "type": "string",
          "description": "The numeric ID of the user, if they are logged in."
        },
        "enteredIdentifier": {
          "type": "string",
          "description": "The phone number or UID the user entered to start the chat."
        },
        "status": {
          "type": "string",
          "description": "The current status of the chat request.",
          "enum": ["pending", "active", "closed"]
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when the chat request was created."
        },
        "chatHistory": {
          "type": "array",
          "description": "The full conversation history with the AI before escalating.",
          "items": {
            "type": "object",
            "properties": {
              "text": { "type": "string" },
              "isUser": { "type": "boolean" },
              "timestamp": { "type": "number" }
            }
          }
        }
      },
      "required": ["enteredIdentifier", "status", "createdAt", "chatHistory"]
    }
  },
  "auth": {
    "providers": ["email", "phone"]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores public profile information for each user."
    },
    "/paymentMethods/{methodId}": {
      "schema": {
        "$ref": "#/entities/AdminPaymentMethod"
      },
      "description": "Stores admin-configured payment methods (Bank or UPI) for users to make payments to."
    },
    "/users/{userId}/orders/{orderId}": {
      "schema": {
        "$ref": "#/entities/Order"
      },
      "description": "Stores user orders to buy LGB."
    },
     "/users/{userId}/sellOrders/{orderId}": {
      "schema": {
        "$ref": "#/entities/SellOrder"
      },
      "description": "Stores user orders to sell LGB."
    },
    "/chatRequests/{requestId}": {
      "schema": {
        "$ref": "#/entities/ChatRequest"
      },
      "description": "Stores user requests to chat with a human agent."
    }
  }
}
